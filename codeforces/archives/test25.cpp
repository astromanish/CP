#include <iostream>
#include <algorithm>
 
using namespace std;
 
int main(void)
{
    string a,b,c,s;
    cin>>a>>b>>c;
    s=a+b;
    sort(c[0],c[-1]);
   // sort(s.begin(),c.end());
    cout<<c<<s;
   /* if(c==s)
    cout<<"YES";
    else
    cout<<"NO";
    return 0; */
}